// const auth = require('./auth');
const shope = require('./shope');
const admin = require('./admin');
const auth = require('./auth');

module.exports = {
	shope: shope,
	admin: admin,
	auth: auth
};
